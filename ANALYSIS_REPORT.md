# Отчет анализа мобильного приложения CleanHouseMobile

**Дата анализа:** 2025-01-20  
**Целевая аудитория:** Курьеры  
**Статус:** Требуются критические исправления

---

## Executive Summary

Мобильное приложение для курьеров имеет базовую функциональность для работы с заказами, но содержит критические проблемы:
- ❌ **Отсутствует проверка роли курьера при логине** - любой пользователь может войти
- ❌ **Отсутствует флоу просроченных заказов** - бекенд поддерживает, но фронтенд не использует
- ⚠️ **Несоответствие типов с бекендом** - price как string вместо number
- ⚠️ **Отсутствуют поля isOverdue и overdueMinutes** в типах заказов

---

## Детальный анализ проблем

### 🔴 CRITICAL (Критические)

| ID | Категория | Расположение | Описание | Рекомендация |
|----|-----------|--------------|----------|--------------|
| C1 | Security | `src/modules/auth/hooks/useVerifySms.tsx:14-50` | Отсутствует проверка роли CURRIER при логине. Любой пользователь (CUSTOMER, ADMIN) может войти в приложение для курьеров | Добавить проверку `if (data.user.role !== UserRole.CURRIER)` и блокировать вход с ошибкой |
| C2 | Security | `app/_layout.tsx:66-70` | Навигация не проверяет роль пользователя перед доступом к защищенным экранам | Добавить проверку роли перед редиректом на `/(protected-tabs)` |

### 🟠 HIGH (Высокий приоритет)

| ID | Категория | Расположение | Описание | Рекомендация |
|----|-----------|--------------|----------|--------------|
| H1 | Missing Feature | `src/modules/orders/types/orders.ts:59-78` | Отсутствуют поля `isOverdue` и `overdueMinutes` в `OrderResponseDto` | Добавить поля согласно бекенду: `isOverdue: boolean; overdueMinutes?: number;` |
| H2 | Missing Feature | `src/modules/orders/api/index.ts:21-43` | API методы не поддерживают параметр `isOverdue` для фильтрации просроченных заказов | Добавить `isOverdue?: boolean` в параметры `findAll` и `findAllNearby` |
| H3 | Missing Feature | `src/screens/main/orders-screen/orders-screen.tsx` | Отсутствует UI для отображения и фильтрации просроченных заказов | Добавить таб/фильтр для просроченных заказов с использованием `isOverdue: true` |
| H4 | Type Mismatch | `src/modules/orders/types/orders.ts:65` | `price: string` не соответствует бекенду, где `price: number` | Изменить тип на `price: number` |
| H5 | Type Mismatch | `src/modules/orders/types/orders.ts:29` | `amount: string` в PaymentDto, на бекенде `amount: number` | Изменить тип на `amount: number` |

### 🟡 MEDIUM (Средний приоритет)

| ID | Категория | Расположение | Описание | Рекомендация |
|----|-----------|--------------|----------|--------------|
| M1 | Inconsistency | `src/modules/orders/types/orders.ts:85-100` | DTO используют `courierId`, но в API используется `currierId` (опечатка) | Унифицировать: использовать `currierId` везде или исправить на бекенде |
| M2 | Missing Feature | `src/modules/orders/hooks/useOrders.tsx:75-144` | Хук `useOrderByLocation` не поддерживает параметр `isOverdue` | Добавить `isOverdue?: boolean` в параметры хука |
| M3 | Error Handling | `src/screens/main/orders-screen/orders-screen.tsx:248-254` | Ошибки загрузки отображаются, но нет детальной информации | Улучшить отображение ошибок с возможностью повтора |
| M4 | Data Sync | `src/modules/orders/hooks/useOrders.tsx:43-45,66-67` | `refetchInterval: 2 * 60 * 1000` - данные обновляются каждые 2 минуты, может быть недостаточно для актуальности | Рассмотреть уменьшение интервала до 30-60 секунд или использовать WebSocket |
| M5 | Type Safety | `src/screens/main/orders-screen/orders-screen.tsx:30` | Используется `any` для типа заказа: `selectedOrder: any` | Заменить на `OrderResponseDto \| null` |

### 🟢 LOW (Низкий приоритет)

| ID | Категория | Расположение | Описание | Рекомендация |
|----|-----------|--------------|----------|--------------|
| L1 | Code Quality | `app/_layout.tsx:30` | Debug console.log в production коде | Удалить или обернуть в условие `__DEV__` |
| L2 | Documentation | `README.md` | Стандартный Expo README, нет информации о функционале для курьеров | Добавить описание функционала приложения |

---

## Анализ покрытия функциональности

### ✅ Реализовано

| Функция | Статус | Качество |
|---------|--------|----------|
| **Авторизация через SMS** | ✅ Реализовано | Хорошо, но без проверки роли |
| **Получение списка заказов** | ✅ Реализовано | Хорошо, с поддержкой локации |
| **Принятие заказа (accept)** | ✅ Реализовано | Хорошо |
| **Начало выполнения (start)** | ✅ Реализовано | Хорошо |
| **Завершение заказа (complete)** | ✅ Реализовано | Хорошо |
| **Отмена заказа (cancel)** | ✅ Реализовано | Хорошо |
| **Детали заказа** | ✅ Реализовано | Хорошо |
| **Обработка ошибок** | ✅ Частично | Базово, можно улучшить |
| **Синхронизация данных** | ✅ Реализовано | Автообновление каждые 2 минуты |

### ❌ Отсутствует

| Функция | Критичность | Описание |
|---------|-------------|----------|
| **Проверка роли CURRIER при логине** | 🔴 CRITICAL | Любой пользователь может войти |
| **Флоу просроченных заказов** | 🟠 HIGH | Бекенд поддерживает, фронтенд нет |
| **Фильтр просроченных заказов** | 🟠 HIGH | Нет UI для просроченных заказов |
| **Отображение информации о просрочке** | 🟠 HIGH | Нет отображения `overdueMinutes` |

---

## Сравнение типов с бекендом

### Несоответствия типов

| Поле | Мобильное приложение | Бекенд | Статус |
|------|----------------------|--------|--------|
| `OrderResponseDto.price` | `string` | `number` | ❌ Несоответствие |
| `PaymentDto.amount` | `string` | `number` | ❌ Несоответствие |
| `OrderResponseDto.isOverdue` | Отсутствует | `boolean` | ❌ Отсутствует |
| `OrderResponseDto.overdueMinutes` | Отсутствует | `number?` | ❌ Отсутствует |
| `OrderResponseDto.scheduledAt` | `string` | `Date \| null` | ⚠️ Разные типы (приемлемо) |

### Отсутствующие поля в API параметрах

| Параметр | Бекенд поддерживает | Мобильное приложение использует |
|----------|---------------------|----------------------------------|
| `isOverdue` | ✅ Да | ❌ Нет |
| `startScheduledAtDate` | ✅ Да | ❌ Нет |
| `endScheduledAtDate` | ✅ Да | ❌ Нет |
| `customerSearch` | ✅ Да | ❌ Нет |

---

## Рекомендации по исправлению

### Немедленные действия (Critical)

#### 1. Добавить проверку роли CURRIER при логине

**Файл:** `src/modules/auth/hooks/useVerifySms.tsx`

```typescript
import { UserRole } from "@/src/shared/api/types/data-contracts";

// В onSuccess после валидации данных:
if (data.user.role !== UserRole.CURRIER) {
    toast.error('Доступ запрещен', {
        description: 'Это приложение предназначено только для курьеров',
        duration: 5000,
    });
    throw new Error('Доступ разрешен только курьерам');
}
```

#### 2. Добавить проверку роли в навигации

**Файл:** `app/_layout.tsx`

```typescript
import { UserRole } from "@/src/shared/api/types/data-contracts";

// В useEffect перед редиректом:
if (userMe?.role === UserRole.CURRIER) {
    router.replace("/(protected-tabs)");
} else if (userMe?.role) {
    // Пользователь не курьер - показываем ошибку
    toast.error('Доступ запрещен', {
        description: 'Это приложение предназначено только для курьеров',
    });
    // Очищаем токены и редиректим на логин
    await removeToken();
    await removeRefreshToken();
    router.replace("/(auth)");
}
```

### Краткосрочные улучшения (High Priority)

#### 3. Обновить типы заказов

**Файл:** `src/modules/orders/types/orders.ts`

```typescript
export interface OrderResponseDto {
  // ... существующие поля
  price: number; // было: string
  scheduledAt?: string | null; // добавить null
  isOverdue: boolean; // новое поле
  overdueMinutes?: number; // новое поле
}

export interface PaymentDto {
  // ... существующие поля
  amount: number; // было: string
}
```

#### 4. Добавить поддержку isOverdue в API

**Файл:** `src/modules/orders/api/index.ts`

```typescript
findAll: (params?: {
  page?: number;
  limit?: number;
  status?: OrderStatus;
  customerId?: string;
  currierId?: string;
  isOverdue?: boolean; // добавить
}) => // ...

findAllNearby: (params: {
  lat: number;
  lon: number;
  maxDistance?: number;
  page?: number;
  limit?: number;
  currierId?: string;
  status?: OrderStatus;
  isOverdue?: boolean; // добавить
}) => // ...
```

#### 5. Добавить флоу просроченных заказов

**Файл:** `src/screens/main/orders-screen/orders-screen.tsx`

- Добавить новую табу "Просроченные"
- Добавить запрос с `isOverdue: true`
- Отображать информацию о просрочке (`overdueMinutes`)

### Среднесрочные улучшения (Medium Priority)

1. Улучшить обработку ошибок с детальной информацией
2. Уменьшить интервал обновления данных или использовать WebSocket
3. Исправить типы с `any` на конкретные типы
4. Унифицировать использование `currierId` vs `courierId`

---

## Метрики качества

| Метрика | Значение | Целевое значение |
|---------|----------|-------------------|
| **Критических проблем** | 2 | 0 |
| **Высокоприоритетных проблем** | 5 | 0 |
| **Среднеприоритетных проблем** | 5 | < 3 |
| **Покрытие функциональности** | 80% | 100% |
| **Соответствие типов с бекендом** | 60% | 100% |
| **Проверка безопасности** | 0% | 100% |

---

## Следующие шаги

### Приоритет 1 (Критично - немедленно)
1. ✅ Добавить проверку роли CURRIER при логине
2. ✅ Добавить проверку роли в навигации

### Приоритет 2 (Высокий - в течение недели)
3. ✅ Обновить типы заказов (добавить isOverdue, overdueMinutes)
4. ✅ Исправить типы price и amount (string → number)
5. ✅ Добавить поддержку isOverdue в API методы
6. ✅ Реализовать UI для просроченных заказов

### Приоритет 3 (Средний - в течение месяца)
7. Улучшить обработку ошибок
8. Оптимизировать синхронизацию данных
9. Убрать использование `any` типов
10. Улучшить документацию

---

## Заключение

Мобильное приложение для курьеров имеет хорошую базовую функциональность, но содержит **критические проблемы безопасности** (отсутствие проверки роли) и **неполную реализацию** функционала просроченных заказов, который уже поддерживается бекендом.

**Рекомендация:** Исправить критические проблемы безопасности перед развертыванием в production.

**Общая оценка готовности:** 6/10

---

*Отчет сгенерирован автоматически на основе анализа кодовой базы*
